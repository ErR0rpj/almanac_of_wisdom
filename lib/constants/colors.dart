import 'package:flutter/material.dart';

class AppColors {
  //Used in background, bottom navigation and other default things
  static const Color primaryColor = Color(0xFFFFFFFF);
  static const Color secondaryColor = Color(0xFF717075);
  static const Color tertiaryColor = Color.fromARGB(255, 107, 109, 114);

  //Used in text colors
  static const Color negativeColor = Colors.black;
  static const Color accentColor = Color(0xFF21529D);

  static const Color textFieldBackgroundColor = Color(0xFFF9F9FB);
}
